
spring.application.name=zuul-proxy-oauth-server
server.port= 8762

eureka.client.service-url.defaultZone: http://localhost:8761/eureka/
zuul.sensitiveHeaders

#zuul.routes.

zuul.routes.authentication.url=http://localhost:8100

ribbon.eureka.enabled=false
 
spring.main.allow-bean-definition-overriding=true


spring.datasource.driver-class-name=com.mysql.jdbc.Driver

spring.datasource.url = jdbc:mysql://localhost:3306/auth?createDatabaseIfNotExist=true&sessionVariables=sql_mode=''&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username = root
spring.datasource.password = surekha
# Preetpal@355

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Setting timezone for Database
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update 
spring.jpa.show-sql=false    